# Runtime deps for Vercel. Matches pyproject.toml; versions pinned from uv.lock.
# Do not add pytest here — it bloats the function bundle.
fastapi==0.141.1
httpx==0.28.1
base58==2.1.1
typer==0.27.1
rich==15.0.0
uvicorn==0.52.4
